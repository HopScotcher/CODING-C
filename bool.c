#include <stdio.h>
#include <stdbool.h>

int main(){
    // bool itsHot = true;

    // printf("is it hot? (1 is true, 0 is false)%d\n",itsHot);
    int x = 100;
     int y = x %= 6;

     printf("y = %d",y);
    return 0;
}